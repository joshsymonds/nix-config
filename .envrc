source_up_if_exists
dotenv_if_exists .env
# Auto-reload stays on: the direnv-prewarm user unit
# (home-manager/direnv-prewarm) re-warms the nix-direnv cache in the
# background whenever flake.nix/flake.lock change, so cd normally hits a
# fresh cache and is instant.
use flake
