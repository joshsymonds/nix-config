source_up_if_exists
dotenv_if_exists .env

# Keep this shell isolated from the root flake's full input graph. `use flake`
# archives every input for GC roots, which makes an unrelated private input
# bump fetch that repository before this tiny shell can load.
watch_file flake.lock dev-shell.nix
use nix
